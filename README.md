SINHAR - Prototype

Local prototype for checking persyaratan pembatalan & pelimpahan porsi haji.

To initialize (SQLite):

1. Ensure PHP is available. Run `php init_db.php` to create DB.
2. Open `http://localhost/sinhar1/` in browser.

Admin: `admin.php` (default admin/admin123)
